# Training data — 1.3 GB, never needed at serving time
data/

# MLflow local stores — the frozen bundle replaces them
mlruns/
mlartifacts/

# Frontend deps and build output (rebuilt inside the image)
web/node_modules/
web/dist/

# Local Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Not copied by either image. consumer.py, producer.py and API/ are kept in the
# context because Dockerfile.pipeline builds them.
docker-compose.yml
train_fraud_model.py
scripts/
tests/

# Docs and misc
*.md
*.png
.git/
.gitignore
.DS_Store
.claude/
